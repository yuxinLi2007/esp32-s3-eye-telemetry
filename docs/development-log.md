# 开发复盘（0 → 1）

记这份东西是因为：这个项目里真正花时间的不是"把功能写出来"，而是**发现自己的判断错了**。

下面按 11 个阶段记录，每阶段四件事：**做了什么 / 遇到什么坑 / 怎么解决 / 验证结果**。
验证结果一栏写的是实际观测到的数字或现象，不是"应该没问题"。

---

## 阶段 1 · 项目骨架与依赖

**做了什么.** 建立 `firmware/` 与 `server/` 两个顶层目录；声明服务端依赖
（`fastapi`、`uvicorn`、`pydantic`、`httpx`）；写 `.gitignore`。

**坑.** 凭据文件的路径有多种可能约定（`secrets.h` 放在 `include/` 还是 `src/`，
环境变量放 `.env` 还是别的名字）。只写一条规则很容易漏。

**解决.** 与其猜，不如把几种常见位置都列上，并**实际验证规则生效**。

**验证结果.** `git check-ignore -v` 确认 `firmware/include/secrets.h`（`.gitignore:2`）
与 `server/.env`（`.gitignore:4`）都被拦下。

> 顺序上先写 `.gitignore` 再写第一行业务代码，是刻意的：密钥一旦进了 git 历史，
> 清理起来非常麻烦，而且如果已经推到远端就几乎无法真正收回。

---

## 阶段 2 · 服务端：存储层与查询接口

**做了什么.** 两张表 `batches`（每次成功上传 = 一条溯源记录）与 `readings`（逐样本带 `seq`）；
`trust_level()` 按 NTP 状态与同步年龄派生可信度；`insert_batch()` / `query_readings()` /
`find_gaps()` / `get_status()`；对应四个 HTTP 接口。

**两个关键设计决定。**

1. **`trust_level` 不落库**，由 `ntp_synced` + `ntp_sync_age_s` 现场算出。
   落库就意味着同一个事实存两份，早晚漂移。
2. **`seq_first` / `seq_last` 由服务端从 `readings` 推导**，不采信客户端上报。
   这样即使传输途中批次被截断，缺口也会在下次查询时真实暴露。

**坑.** 一开始没想到"缺口"有两种性质完全不同的成因：同一个 `boot_id` 内 `seq` 跳号
（确凿丢样）和跨 `boot_id`（设备重启）。混在一起报会得出错误结论。

**解决.** `find_gaps()` 只报同 `boot_id` 内的跳号；跨 `boot_id` 另外统计为"重启次数"。

**验证结果.** 14 项测试通过，覆盖往返查询、可信度四档、缺口检测、重启不误判、
丢弃计数可见性。

---

## 阶段 3 · 板端 PlatformIO 工程配置

**做了什么.** `platformio.ini` 板型覆盖。

**坑（两个，都会导致"能编译但完全没反应"）.**

1. **ESP32-S3-EYE 没有官方 PlatformIO 板型。** 用 `esp32-s3-devkitc-1` 作基底，
   但两者硬件不同：EYE 是 **ESP32-S3-WROOM-1-N8R8**（8MB flash + 8MB **Octal** PSRAM），
   而 devkitc-1 默认按 `qio_qspi` / 无 PSRAM 处理。
2. **串口指向了没接线的引脚。** EYE 的 USB 口走 GPIO19/20 原生 USB（枚举为
   `303A:1001` USB-Serial/JTAG），而 Arduino 默认把 `Serial` 指向 UART0（GPIO43/44）
   ——那对脚根本没接到 USB 口。

**解决.** `board_build.arduino.memory_type = qio_opi` + `board_build.partitions = default_8MB.csv`；
`build_flags` 加 `ARDUINO_USB_MODE=1` 与 `ARDUINO_USB_CDC_ON_BOOT=1`。

**验证结果.** 编译通过，`pio device monitor` 能正常打印日志。

---

## 阶段 4 · 板端采集、环形缓冲与上报

**做了什么.** I2C 加速度计读取、I2S 麦克风算声压级、环形缓冲（240 个 ≈ 12 秒）、
HTTP 批量上报；引入 `boot_id`（每次启动随机）、`seq`（本次启动内自增）、
`dropped_since_last`（缓冲溢出丢弃计数）、NTP 状态上报。

**坑（三个，其中两个是"看起来有数据但其实是错的"）.**

1. **想当然用 PDM API。** 按 PDM 处理麦克风、写了 `i2s_pdm.h`。实际这块板子是
   **标准 I2S**，而且 Arduino-ESP32 核心是 **2.0.17（不是 3.x）**，只有遗留的
   `driver/i2s.h`，`i2s_std.h` / `i2s_pdm.h` 根本不存在。
2. **位宽读错，得到"对声音无反应的噪声"。** 麦克风输出的是"32 位帧内的 24 位数据"，
   必须按 32 位读再右移 8 位。按 16 位读会帧错位——**看起来像有数据，其实是噪声**，
   比直接报错更难发现。
3. **加速度计量程抄了数据手册，错了 61%。** 初值按手册取 `8192 LSB/g`，
   实测静止时 `|a| = 1.610 g`。

**解决.** 改用 `driver/i2s.h` 的 `I2S_BITS_PER_SAMPLE_32BIT` + `I2S_CHANNEL_FMT_ONLY_LEFT`，
真实样本 = `raw32 >> 8`；量程改为 `13189`（8192 × 1.610 实测反推）。

**验证结果.**

- 修正量程后实测 `|a| = 1.004`（1000 样本内 0.989–1.041）
- 麦克风：静室底噪约 -70 dBFS ⇒ 50 dB SPL；拍手约 -38 dBFS ⇒ 82 dB SPL

**遗留局限（写进了注释和 README，没有掩盖）.** 单姿态标定无法把量程误差和零偏误差分开。
当前值只保证本姿态正确；换姿态若明显偏离 1，需要多姿态拟合。

> 这一阶段之后，凡是要用新的硬件 API，都先编译验证再写逻辑——见阶段 6 的 NTP。

---

## 阶段 5 · 上机联调：跨线程 500

**做了什么.** 真实设备接入服务端，挂载静态界面。

**坑（这个项目里最有价值的一个）.**

`/api/v1/status` 在真实并发下间歇性返回 500：

```
sqlite3.ProgrammingError: SQLite objects created in a thread
can only be used in that same thread
```

根因：**FastAPI 把同步依赖和同步端点分别丢进线程池**，两者不保证落在同一个线程上，
而数据库连接是在依赖里创建的。

**为什么 14 项测试全绿却没抓到.** `TestClient` 是串行的，恰好复用了同一个线程。
测试环境的"恰好"掩盖了真实环境的并发模型。

**解决.** `check_same_thread=False`。安全性论证：连接每请求新建、用完即关，
依赖先于端点返回，所以只是**跨线程先后使用**，不存在并发访问。

**验证结果.** 36 个并发请求 **36/36 返回 200**。

> **教训：测试通过 ≠ 正确。** 这一条后来直接影响了验证策略（阶段 8）。

---

## 阶段 6 · 时间戳可信度：让 NTP 说真话

**做了什么.** 重写 NTP 判定逻辑，让"同步年龄"反映**真实收到的 SNTP 应答**。

**坑（两个，性质不同）.**

1. **"新鲜"是自己说的，没有证据。** 原逻辑：把 `g_ntp_synced = false`，调 `configTime()`，
   再读一次 `time()` 看值合不合理，合理就标记同步成功。问题在于 `time()` **必然**合理
   ——系统时钟从上次同步起一直在走。于是每次"重同步"都把年龄清零，**把"没有收到任何
   应答"报成"刚刚同步过"**。即"新鲜"这个结论本身不可信。
2. **两个阈值之间没有对照检查，标签退化成常量。** `NTP_RESYNC_MS` 原本 30 分钟，
   而服务端判定"新鲜"的阈值是 300 秒。30 分钟 ≫ 5 分钟，意味着年龄在涨到 30 分钟的
   过程中**必然**越过 300 秒。

**解决.** 同步只认 SNTP 回调（`sntp_set_time_sync_notification_cb`），重同步用
`sntp_restart()` 强制重新查询；`NTP_RESYNC_MS` 改为 4 分钟，留出重试与回调延迟余量。

**验证结果.**

- 同步年龄呈锯齿波：0 → 240s 增长，收到应答后归零
- **0 个批次的同步年龄超过 300 秒**（改之前是 3000/3000 全被判 `ntp_stale`）

> 这里**先编译验证了 `esp_sntp.h` 和 `sntp_restart()` 确实存在**，没有凭印象写——
> 因为刚在阶段 4 的 I2S API 上吃过高估可用性的亏。

---

## 阶段 7 · 界面可读性：两处缺陷

**做了什么.** 重排报警优先级链；修正 `|a|` 纵轴。

**坑（一个引发另一个）.**

1. **界面自己打自己的脸。** 主报警条写"链路正常……时间戳可信"，下面又附了一行
   "⚠ 窗口内存在非新鲜时间戳"。两个相反结论同屏出现。根因是可信度被当成"附录"追加，
   而不是进入报警优先级链。
2. **纵轴把真实信号压平了。** `|a|` 图用 `forceMin: 0.5` 当纵轴地板，浪费了约 60% 的
   面板高度，把 0.99–1.01 的真实波动压成一条直线——等于没画。
3. **修完 2 立刻暴露 3：刻度精度。** 量程窄时固定 3 位小数会把 5 条刻度印成
   `1.000 / 1.000 / 1.000 / 0.999 / 0.999`，纵轴同样没有信息。而且**设备静止时跨度
   就只有千分之几，这是常态不是异常**。

**解决.** 可信度并入报警主链、删掉附注行；`forceMin` 改为 `include: [1.0]`
（参考值参与量程计算保证在视野内，但不当地板）；刻度精度按实际刻度间隔自适应，
位数不够时加宽左侧留白。

**验证结果.** 无头 Edge 截图逐一确认：报警条不再自相矛盾；`|a|` 的五条刻度
在静止状态下可区分；曲线能看到真实波动。

---

## 阶段 8 · 验证工具：主动制造失败

**做了什么.** 两个工具，都进了仓库。

- `server/tools/fault_inject.py` — 对着隔离实例注入 4 种缺陷：`seq` 缺口、
  `dropped_since_last > 0`、NTP 未同步、彻底静默。
- `server/tools/ui_check.mjs` — 把 `index.html` 里的脚本抠出来，用最小 DOM 桩在 node 里跑，
  断言报警等级与文案、卡片数值、分段结果。

**为什么必须这么做.** 第 3 条要求（失败必须在界面上被看见）是**否定性**的——
正常情况下永远看不到。所以验证不能靠"我觉得逻辑写对了"，必须主动制造失败。
阶段 5 也证明了"测试全绿"本身不足为凭。

**坑（工具自身的，两个）.**

1. DOM 桩漏了 `<select>` 的 `value` 属性 → 请求变成 `?limit=undefined` → 422。
2. node 环境没有 origin，界面用的相对路径解析失败 → 补一个把相对路径拼成绝对地址的
   fetch 包装。

**验证结果.** 4 种故障分别触发对应的报警等级与文案；界面脚本在无浏览器环境下
可确定性回归。

---

## 阶段 9 · 入库鉴权

**做了什么.** `X-Ingest-Token` 请求头 + `secrets.compare_digest` 定时安全比较。

**坑 / 设计取舍.** 用 `compare_digest` 而不是 `==`：逐字节比较会从耗时上泄漏密钥前缀。
另外，服务端**未配置令牌时不拦截**（本地开发友好），但启动时打印警告。

**验证结果.** 5 项鉴权测试：缺令牌 401、错令牌 401、对令牌 201、
**被拒的请求不留下任何数据**、读接口仍然公开。

---

## 阶段 10 · 采集开关 + `file://` 直开

**做了什么.** `control_log` 表（只记变更，当前状态取最后一条）；三个控制接口；
界面开始/停止按钮；板端每 2 秒轮询开关。

**坑（三个，核心是"停止"和"故障"必须能区分）.**

1. **停止后界面安静下来，和"设备失联"在图上长得一模一样。** 这**直接违背第 3 条要求**
   ——把用户自己的操作报成故障，比不报更糟。
2. **修完 1 又发现同屏矛盾。** 卡片"最近一批距今"在停止状态下仍然标红，
   和上方蓝色的"已停止采集"提示互相打脸（和阶段 7 的坑 1 是同一类错误）。
3. **反向风险：不能把真实失灵也归进中性提示。** 如果停止指令下发之后板子还在上传，
   这说明设备没执行指令——一旦被中性色盖过去，一次真实的失灵就被描述成用户的操作。

**解决.**

- 报警链新增中性蓝"已停止采集"；图上空白画成**灰色**带并标注
- 停止区间从 `control_log` 历史配对出来，用来把"上传中断"识别成"主动停止"
- 卡片数字照报但不着色
- **反向兜底**：若停止指令下发 14 秒后仍在收到新批次，报
  `a-warn「已下发停止，但仍在收到数据」`
- 板端默认采集；**轮询不到服务端时维持现状**，不因一次网络抖动自行停采
- 停止只停"采样"、不停"上传"，缓冲区里已采到的样本照常传完

**验证结果.**

| 项目 | 结果 |
|---|---|
| 真机停止 | `readings` 冻结、`state=0`、缓冲区排空后彻底安静 |
| 停止后恢复 | **`seq` 无跳号，服务端缺口判定为 0**（停止不会被误报成丢数据） |
| "板子不理会停止指令" | 独立实例实测（停 → 等 16 秒 → 灌一批），界面正确报 `a-warn`，未被中性提示掩盖 |
| `file://` 渲染 | 无头 Edge 截图确认图表、来源面板、报警条均正确 |
| 跨源请求 | `Origin: null` 的预检与 POST 均返回正确 CORS 头 |

---

## 阶段 11 · 整理与交付

**做了什么.** 补 `README.md`、`docs/development-log.md`、配置模板
（`secrets.example.h` / `.env.example`）、`start_server.bat`；补全 `.gitignore`；
做全仓泄漏检查。

**坑（三个，都是环境层面的）.**

1. **shell 把含中文的 JSON body 搞坏** → 服务端报
   `There was an error parsing the body`。改用 UTF-8 文件 + `--data-binary @file`。
2. **git-bash 的 `/tmp` 与原生 Python 的 `/tmp` 不是同一个路径** → Python 报
   `FileNotFoundError: '\tmp\msg.txt'`。改用 Windows 绝对路径，或全程用 Python 的
   `subprocess` 调 git。
3. **发现自己上一个提交带了 `Co-Authored-By` 署名** → 用 `git commit --amend -F` 去掉。

**泄漏检查方法.** 写脚本从磁盘读取真实凭据，再在整个仓库搜索——**脚本不打印密钥本身**
（打印出来就等于又泄一次），只报"哪个变量在哪个文件里出现"。

**验证结果.**

| 检查项 | 结果 |
|---|---|
| 已跟踪文件（19 个） | 无任何敏感值 |
| 全部 git 历史（134707 字符） | 敏感值**从未进入过任何提交** |
| 私网 IP | 0 处（唯一命中是模板里的占位符 `192.168.1.100`，与真实 IP 不同） |
| 密码/令牌赋值模式 | 0 处 |
| 忽略规则边界 | 模板可入库、真凭据被拦住 |
| AI 署名 | 全历史 **0 个** |
| 测试 | 27 项通过 |

---

## 横向：三条一直用到的判断准则

### 1. 同一个事实不要存两处

`trust_level` 由 `ntp_synced` + `ntp_sync_age_s` 现场算出，**不落库成字段**；
采集开关的当前状态取 `control_log` 的最后一条，**不另设单行表**。

理由一样：存两份，早晚会漂移，而漂移之后没有权威可依据。

### 2. 派生值只能有一个来源

界面里的 `trustOf()` 与服务端的 `trust_level()` 是同一套规则写了两遍。这是刻意的取舍
——前端不能依赖后端每次都算好（逐点打标要用行内字段）。代价是**改阈值要改两处**，
换共享配置就得引入构建步骤，对一个单文件、无构建的界面来说不划算。
两边的常量都加了"与某某保持一致"的注释互相指名。

### 3. 不确定就实测，别抄文档

`ACCEL_LSB_PER_G`、`MIC_DBFS_TO_SPL`、`NTP_RESYNC_MS` 全都是实测定出来的，
注释里写明了怎么测的、初值多少、为什么改。硬件项目里"数据手册说 8192"和
"这块板子实际是 13189"是两回事。

---

## 验证方法总述

这套方法确实起了作用：阶段 5（测试全绿但真实并发 500）、阶段 6（NTP 假新鲜）、
阶段 7（三处界面缺陷）**没有一处是靠读代码发现的**。

| 手段 | 用途 |
|---|---|
| `pytest`（27 项） | 存储、接口、鉴权、缺口判定的回归 |
| `fault_inject.py` | 主动注入 4 种缺陷，验证"失败可见" |
| `ui_check.mjs` | DOM 打桩跑界面脚本，确定性断言报警判定 |
| 无头 Edge 截图 | 确认界面**真的渲染正确**，而不只是代码看起来对 |
| 独立实例（8001 + 独立库） | 测危险路径而不污染真实数据 |
| 全仓泄漏扫描 | 推送前确认无凭据入历史 |

---

## 如果重来

1. **一开始就把"失败可见"当成可测需求**，而不是写完再想怎么验证。
   故障注入器应该和存储层同期出现，而不是等到阶段 8。
2. **相关阈值放在一起定义。** 阶段 6 的坑 2（30 分钟 vs 300 秒）如果两个常量写在同一处，
   写的时候就发现了。
3. **对硬件 API 先编译验证再写逻辑。** 阶段 4 的 PDM 教训。
4. **警惕"看起来对"的中间结果。** 阶段 4 的位宽错误、阶段 6 的假新鲜，都属于
   "程序在跑、有输出、但输出是错的"，比崩溃难发现得多。

---

## 仍未解决

- **有效采样率约 16.6 Hz**（配置 20 Hz）。每轮循环有一次阻塞式 I2S 读取（约 16 ms）。
  `t_device_ms` 如实记录所以数据不误导，但抖动未处理。
- **断电时未上传的缓冲样本不计入任何计数器**，只能表现为 `boot_id` 变化
  加上服务端接收时刻的空白。
- **固件尚不支持 HTTPS**，`SERVER_URL` 只能填 `http://`，令牌明文上路。
- **`/api/v1/readings` 的 `since_ms` 跨重启无意义**（过滤的是设备启动内毫秒数）。
  界面刻意不用它，但接口仍然敞着——是个陷阱。
- **`control_log` 无清理机制**，低频操作下不是问题。
- **VPS 部署尚未实测**，README 中给出的是待验证方案。
